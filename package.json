{
  "name": "hously-monorepo",
  "private": true,
  "packageManager": "bun@1.3.11",
  "scripts": {
    "install:all": "bun install",
    "build": "bun run build:web",
    "build:web": "bun run --filter @hously/web build",
    "dev": "bun run --workspaces --if-present dev",
    "dev:api": "bun run --filter @hously/api dev",
    "dev:web": "bun run --filter @hously/web dev",
    "dev:services": "docker compose up db redis -d",
    "docker:down": "docker compose down",
    "docker:rebuild": "docker compose build --no-cache",
    "test": "bun run --filter @hously/web test && bun run --filter @hously/api test && bun run --filter @hously/shared test",
    "format": "bun run --workspaces --if-present format",
    "formatCheck": "bun run --workspaces format -- --check",
    "typecheck": "bun run --workspaces --if-present typecheck",
    "typecheck:build": "tsc -b tsconfig.build.json --pretty false",
    "typecheck:native": "bun run --workspaces --if-present typecheck:native",
    "typecheck:native:build": "tsgo -b tsconfig.build.json --pretty false",
    "lint": "bun run --filter @hously/web lint && bun run --filter @hously/api lint",
    "knip": "knip",
    "duplicates": "bunx jscpd apps/web/src apps/api/src apps/shared/src --min-lines 20 --min-tokens 100 --gitignore --ignore \"**/*.test.*,**/*.spec.*,**/__tests__/**,**/__fixtures__/**,**/*.d.ts,**/generated/**\" --reporters console",
    "clean": "rm -rf apps/web/dist node_modules apps/*/node_modules && docker compose down -v",
    "db:migrate:dev": "bun run --filter @hously/api db:migrate:dev",
    "db:migrate:deploy": "bun run --filter @hously/api db:migrate",
    "db:migrate:push": "bun run --filter @hously/api db:push",
    "db:generate": "bun run --filter @hously/api db:generate",
    "db:studio": "bun run --filter @hously/api db:studio",
    "db:refresh-collation": "sh -c 'DB_NAME=${DB_NAME:-hously}; DB_USER=${DB_USER:-hously}; echo \"Refreshing template1 collation version...\"; docker compose exec -T db psql -U \"$DB_USER\" -d postgres -c \"ALTER DATABASE template1 REFRESH COLLATION VERSION;\"; echo \"Refreshing database collation version for database: $DB_NAME...\"; docker compose exec -T db psql -U \"$DB_USER\" -d postgres -c \"ALTER DATABASE $DB_NAME REFRESH COLLATION VERSION;\"; echo \"Done.\"'",
    "prepare": "husky"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "@eslint/eslintrc": "^3.3.5",
    "@eslint/js": "^10.0.1",
    "@types/react": "~19.2.14",
    "@typescript/native-preview": "^7.0.0-dev.20260506.1",
    "elysia": "^1.4.28",
    "eslint-plugin-no-hardcoded-strings": "^1.1.0",
    "husky": "^9.1.7",
    "knip": "^6.12.0",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.100.9",
    "date-fns": "^4.1.0",
    "i18next": "^26.0.9",
    "react": "19.2.6",
    "react-hook-form": "^7.75.0",
    "react-i18next": "^17.0.6",
    "recharts": "3.8.1",
    "sharp": "^0.34.5"
  },
  "version": "1.0.0"
}
